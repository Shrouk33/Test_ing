class backpain {
  String ? Catpose;
  String ? Toe ;
  // Add any other fields you need
  backpain.fromMap(Map<String, dynamic> map) {
    Catpose = map ['Catpose'];
    Toe = map['Toe'];
    // Assign any other fields you need
  }
}
